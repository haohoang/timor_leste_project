
SELECT
    ID,
  MSISDN,
  DEPOSIT_AMOUNT,
  BONUS_AMOUNT,
  TYPE,
  DATE_CREATED,
  STATUS,
  ERR_CODE,
  ERR_DES,
  TRANS_ID,
  CARRIED_ACC_ID,
  IMEI
FROM
    LOG_BONUS_DEPOSIT
WHERE
    ID >= TRUNC(SYSDATE - 1)
            AND ID < TRUNC(SYSDATE)
        